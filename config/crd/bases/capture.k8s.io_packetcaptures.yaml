---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: packetcaptures.capture.k8s.io
spec:
  group: capture.k8s.io
  names:
    kind: PacketCapture
    listKind: PacketCaptureList
    plural: packetcaptures
    shortNames:
    - pc
    singular: packetcapture
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.duration
      name: Duration
      type: string
    - jsonPath: .status.packetsCaptured
      name: Packets
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PacketCapture is the Schema for the packetcaptures API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PacketCaptureSpec defines the desired state of PacketCapture
            properties:
              destination:
                description: Destination defines the destination endpoint selector
                properties:
                  cidr:
                    description: CIDR specifies an IP address range in CIDR notation
                    items:
                      type: string
                    type: array
                  fqdn:
                    description: FQDN specifies fully qualified domain names
                    items:
                      type: string
                    type: array
                  namespaceSelector:
                    description: NamespaceSelector selects namespaces by labels
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  podSelector:
                    description: PodSelector selects pods by labels
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  ports:
                    description: Ports specifies port numbers or ranges
                    items:
                      description: PortSelector defines port selection criteria
                      properties:
                        port:
                          description: Port is a single port number
                          format: int32
                          type: integer
                        portRange:
                          description: PortRange defines a range of ports (e.g., "8000-9000")
                          type: string
                        protocol:
                          description: Protocol is the transport protocol (TCP, UDP,
                            SCTP)
                          enum:
                          - TCP
                          - UDP
                          - SCTP
                          type: string
                      type: object
                    type: array
                type: object
              duration:
                description: Duration specifies how long to capture packets (e.g.,
                  "5m", "1h")
                pattern: ^([0-9]+(\.[0-9]+)?(ns|us|µs|ms|s|m|h))+$
                type: string
              filter:
                description: Filter is a BPF filter expression (tcpdump syntax)
                type: string
              interface:
                default: any
                description: 'Interface specifies the network interface to capture
                  on (default: any)'
                type: string
              maxPacketSize:
                default: 65535
                description: MaxPacketSize limits the snapshot length of each packet
                  in bytes
                type: integer
              maxPackets:
                default: 0
                description: MaxPackets limits the number of packets to capture (0
                  = unlimited)
                type: integer
              nodeSelector:
                additionalProperties:
                  type: string
                description: NodeSelector allows targeting specific nodes for capture
                type: object
              source:
                description: Source defines the source endpoint selector
                properties:
                  cidr:
                    description: CIDR specifies an IP address range in CIDR notation
                    items:
                      type: string
                    type: array
                  fqdn:
                    description: FQDN specifies fully qualified domain names
                    items:
                      type: string
                    type: array
                  namespaceSelector:
                    description: NamespaceSelector selects namespaces by labels
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  podSelector:
                    description: PodSelector selects pods by labels
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  ports:
                    description: Ports specifies port numbers or ranges
                    items:
                      description: PortSelector defines port selection criteria
                      properties:
                        port:
                          description: Port is a single port number
                          format: int32
                          type: integer
                        portRange:
                          description: PortRange defines a range of ports (e.g., "8000-9000")
                          type: string
                        protocol:
                          description: Protocol is the transport protocol (TCP, UDP,
                            SCTP)
                          enum:
                          - TCP
                          - UDP
                          - SCTP
                          type: string
                      type: object
                    type: array
                type: object
              storage:
                description: Storage defines where to store captured packets
                properties:
                  gcs:
                    description: GCS configuration for Google Cloud Storage
                    properties:
                      bucket:
                        description: Bucket name
                        type: string
                      credentialsSecret:
                        description: CredentialsSecret references a secret containing
                          GCS credentials
                        type: string
                      prefix:
                        description: Prefix for object keys
                        type: string
                    required:
                    - bucket
                    type: object
                  persistentVolumeClaim:
                    description: PersistentVolumeClaim name for PersistentVolume storage
                    type: string
                  retentionDays:
                    default: 7
                    description: RetentionDays specifies how long to keep capture
                      files
                    type: integer
                  s3:
                    description: S3 configuration for S3 storage
                    properties:
                      bucket:
                        description: Bucket name
                        type: string
                      credentialsSecret:
                        description: CredentialsSecret references a secret containing
                          AWS credentials
                        type: string
                      prefix:
                        description: Prefix for object keys
                        type: string
                      region:
                        description: Region of the S3 bucket
                        type: string
                    required:
                    - bucket
                    type: object
                  type:
                    default: PersistentVolume
                    description: Type specifies the storage backend type
                    enum:
                    - PersistentVolume
                    - S3
                    - GCS
                    - ConfigMap
                    type: string
                required:
                - type
                type: object
            required:
            - duration
            type: object
          status:
            description: PacketCaptureStatus defines the observed state of PacketCapture
            properties:
              bytesCaptured:
                description: BytesCaptured is the total bytes captured
                format: int64
                type: integer
              captureFiles:
                description: CaptureFiles lists the generated capture files
                items:
                  type: string
                type: array
              captureJobs:
                description: CaptureJobs tracks the jobs created for this capture
                items:
                  description: CaptureJobStatus tracks individual capture jobs per
                    node
                  properties:
                    captureFile:
                      description: CaptureFile path or URL
                      type: string
                    jobName:
                      description: JobName is the name of the Kubernetes Job
                      type: string
                    nodeName:
                      description: NodeName is the node where this capture is running
                      type: string
                    packetsCaptured:
                      description: PacketsCaptured on this node
                      format: int64
                      type: integer
                    status:
                      description: Status is the current status of this job
                      type: string
                  required:
                  - jobName
                  - nodeName
                  - status
                  type: object
                type: array
              conditions:
                description: Conditions represent the latest available observations
                  of the capture's state
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              endTime:
                description: EndTime is when the capture completed or failed
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  state
                type: string
              packetsCaptured:
                description: PacketsCaptured is the total number of packets captured
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the packet capture
                enum:
                - Pending
                - Running
                - Completed
                - Failed
                type: string
              startTime:
                description: StartTime is when the capture started
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
